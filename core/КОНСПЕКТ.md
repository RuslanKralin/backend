# Конспект: Пакет @ticket_for_cinema/core

## Что это за пакет?

Это **общий npm пакет с настройками** для всех сервисов проекта `ticket_for_cinema`.

---

## Зачем он нужен?

### Проблема без общего пакета:

- ❌ В каждом сервисе свои настройки (форматирование, линтинг)
- ❌ Код выглядит по-разному в разных проектах
- ❌ Нужно копировать конфиги вручную
- ❌ При изменении настроек - обновлять везде

### Решение с общим пакетом:

- ✅ Единые настройки для всех проектов
- ✅ Изменения в одном месте → применяются везде
- ✅ Новые разработчики сразу получают правильные настройки
- ✅ Код единообразный во всех сервисах

---

## Что сейчас в пакете?

### 📦 Prettier конфигурация

- Единые правила форматирования кода
- Автоматическая сортировка импортов
- Файл: `src/prettier.config.mjs`

---

## Как использовать в других проектах?

### 1. Установка пакета:

```bash
npm install @ticket_for_cinema/core
# или
yarn add @ticket_for_cinema/core
```

### 2. Использование Prettier конфига:

В `package.json` вашего проекта добавьте:

```json
{
  "prettier": "@ticket_for_cinema/core/prettier"
}
```

Или создайте файл `prettier.config.mjs`:

```javascript
export { default } from '@ticket_for_cinema/core/prettier';
```

---

## Автоматическая публикация (CI/CD)

### Что реализовано:

**GitHub Actions workflow** автоматически публикует пакет в npm при каждом push в ветку `main`.

### Как это работает:

**Раньше** (вручную):

1. Изменения в коде
2. `git push`
3. **Вручную** `npm publish`
4. **Вручную** ввод пароля и 2FA

**Теперь** (автоматически):

1. Изменения в коде
2. `git push`
3. **Всё остальное автоматически!** ✨

### Файл workflow:

`.github/workflows/publish.yml`

### Что делает workflow:

1. Устанавливает Node.js
2. Устанавливает зависимости (`npm install`)
3. Создаёт `.npmrc` с токеном
4. Публикует пакет (`npm publish`)

---

## Обновление версии пакета

### Что такое версия?

Версия выглядит так: `1.2.3`

- **1** - мажорная (большие изменения, могут сломать код)
- **2** - минорная (новые функции, всё работает)
- **3** - патч (исправления багов)

### Как обновить версию:

#### Шаг 1: Закоммитить изменения

```bash
git add .
git commit -m "Описание изменений"
```

#### Шаг 2: Обновить версию

```bash
# Патч (1.0.0 → 1.0.1) - исправили баг
npm version patch

# Минор (1.0.0 → 1.1.0) - добавили функцию
npm version minor

# Мажор (1.0.0 → 2.0.0) - большие изменения
npm version major
```

Команда автоматически:

- Обновит версию в `package.json`
- Создаст git commit
- Создаст git tag

#### Шаг 3: Запушить изменения

```bash
git push
git push --tags
```

#### Шаг 4: Автоматическая публикация

GitHub Actions автоматически опубликует новую версию! 🚀

---

## Быстрая шпаргалка

### Полный цикл обновления патч-версии:

```bash
git add .
git commit -m "Fix: описание исправления"
npm version patch
git push && git push --tags
```

### Полный цикл обновления минор-версии:

```bash
git add .
git commit -m "Feature: новая функция"
npm version minor
git push && git push --tags
```

---

## Настройка токена NPM

### Где хранится токен:

- GitHub: Settings → Secrets and variables → Actions
- Секрет называется: `NPM_TOKEN`

### Как создать новый токен:

1. Перейти на [npmjs.com](https://www.npmjs.com/)
2. Access Tokens → Generate New Token
3. Выбрать **Granular Access Token**
4. ✅ Поставить галочку **"Bypass two-factor authentication (2FA)"**
5. Permissions: **Read and write**
6. Organizations: выбрать `ticket_for_cinema`
7. Generate Token
8. Скопировать и добавить в GitHub Secrets

---

## Что можно добавить в будущем?

- 📝 ESLint конфигурация (правила линтинга)
- 🔧 TypeScript конфигурация
- 🛠️ Общие утилиты и хелперы
- 🔑 Константы (API URLs, коды ошибок)
- 📘 Типы данных (TypeScript types)
- 🎨 Общие UI компоненты

---

## Эффект от автоматизации

### ✅ Экономия времени

Не нужно каждый раз вручную публиковать пакет

### ✅ Надёжность

Нет риска забыть опубликовать изменения

### ✅ Прозрачность

История всех публикаций видна в GitHub Actions

### ✅ Профессионализм

Стандартная практика в современной разработке (CI/CD)

---

## Полезные ссылки

- **NPM пакет**: https://www.npmjs.com/package/@ticket_for_cinema/core
- **GitHub репозиторий**: https://github.com/ticketForCinema/core
- **GitHub Actions**: https://github.com/ticketForCinema/core/actions

---

## Простыми словами

**Вы создали библиотеку общих настроек для всех проектов `ticket_for_cinema` и настроили робота, который автоматически публикует её в npm при каждом обновлении кода.** 🤖📚
