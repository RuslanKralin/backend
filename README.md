# Backend Project

## Конспект: Решение проблемы с Git Push и что такое SSH

### 🔴 Проблема, которая возникла

Когда ты пытался отправить код на GitHub командой `git push`, появилась ошибка:
```
Invalid username or token. Password authentication is not supported for Git operations.
```

**Простыми словами:** GitHub раньше позволял вводить логин и пароль при отправке кода. Но это небезопасно (пароли легко украсть), поэтому GitHub отключил эту возможность.

---

### 🔑 Что такое SSH

**SSH (Secure Shell)** - это безопасный способ подключения к удаленным компьютерам и сервисам (например, GitHub).

#### Как это работает (простая аналогия):

Представь, что у тебя есть:
- **Замок** (публичный ключ) - его ты даешь GitHub
- **Ключ** (приватный ключ) - он остается только у тебя на компьютере

Когда ты делаешь `git push`:
1. GitHub говорит: "Докажи, что это ты!"
2. Твой компьютер использует приватный ключ (который знаешь только ты)
3. GitHub проверяет его с помощью публичного ключа (который ты ему дал)
4. Если совпадает - доступ разрешен!

**Главное преимущество:** Никто не может украсть твой доступ, даже если перехватит данные. Пароль вводить не нужно!

---

### ✅ Как мы решили проблему

#### Шаг 1: Проверили наличие SSH-ключа
```bash
ls -la ~/.ssh/id_*.pub
```
У тебя уже был ключ: `id_ed25519.pub`

#### Шаг 2: Переключили Git с HTTPS на SSH
**Было:**
```
https://github.com/RuslanKralin/backend.git
```

**Стало:**
```
git@github.com:RuslanKralin/backend.git
```

Команда:
```bash
git remote set-url origin git@github.com:RuslanKralin/backend.git
```

#### Шаг 3: Добавили публичный ключ на GitHub
1. Скопировали содержимое файла `~/.ssh/id_ed25519.pub`
2. Зашли на https://github.com/settings/keys
3. Нажали "New SSH key"
4. Вставили ключ и сохранили

#### Шаг 4: Проверили, что все работает
```bash
git push
```
✅ Успешно! Код отправлен на GitHub.

---

### 📝 Что теперь изменилось

- **Раньше:** При каждом `git push` нужно было вводить логин и пароль
- **Теперь:** `git push` работает автоматически, без ввода пароля
- **Безопасность:** Твой приватный ключ никогда не покидает твой компьютер

---

### 🛠️ Полезные команды

```bash
# Посмотреть настройки удаленного репозитория
git remote -v

# Посмотреть свой публичный SSH-ключ
cat ~/.ssh/id_ed25519.pub

# Проверить подключение к GitHub через SSH
ssh -T git@github.com
```

---

### 💡 Важно запомнить

1. **Публичный ключ** (`.pub`) - можно показывать всем, добавлять на GitHub
2. **Приватный ключ** (без `.pub`) - НИКОГДА никому не показывать, не отправлять, не коммитить в Git!
3. SSH работает не только с GitHub, но и с любыми серверами (например, при подключении к удаленному серверу)

---

### 🎯 Итог

Проблема была в том, что GitHub перестал принимать пароли. Мы настроили SSH-аутентификацию - более безопасный способ, который работает автоматически. Теперь `git push` будет работать без проблем!
